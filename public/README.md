# Static Assets Folder

Place any static assets that you want served directly from the root path here.

To host your Android APK file for download:
1. Upload your `.apk` file to this `public` folder.
2. Rename the file to `piratelink.apk`.

Once uploaded to this path, clicking the "Download standalone APK" button in the application will automatically serve this physical file to visitors!
