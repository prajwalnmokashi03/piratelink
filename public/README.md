# Public Assets Directory

This folder is used to host static assets that should be served at the root URL path.

**Instructions for adding the APK file:**
1. Upload your `piratelink.apk` file directly into this `public` folder on GitHub.
2. Once uploaded, the Download button on the landing page will automatically link to the APK file in this directory.
